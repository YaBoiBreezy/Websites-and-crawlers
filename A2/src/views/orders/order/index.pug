doctype html

html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width")

    title= order.id

  body
    h1= customer.username
    p 
      a(href=`/orders`) Orders
    p 
      span Order ID: #{order.id}
      
    each item in order.orderItems
    p
      span Product ID: #{item.productId}
      span Quantity: #{item.quantity}
